<div class="mural premio" id="<%=premio.id%>">
		<h3><%= premio.nome %></h3>
		<%=link_to image_tag(premio.image), image_path(premio.image) , :rel => "facebox"%>
		<div class="info" >
			<p>Valor: <span id="Valor<%=premio.id%>"><%=premio.valor%></span> pontos</p>
			
			
			<%=concorrer_buttom(premio)%>
		
		</div>
				
		<div class="cf"></div>
</div>

